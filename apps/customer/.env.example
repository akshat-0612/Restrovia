# Which restaurant this build belongs to. This is the ONE value that changes
# per client — everything else (branding, menu, tax) comes from the database.
VITE_RESTAURANT_SLUG=delight-food

# Leave blank in development to use the Vite proxy.
# In production: https://api.yourdomain.com
VITE_API_URL=
